# Plan

## Attack vector coverage matrix

| Attack vector | Known technique | Check needed | Current coverage | Status |
|---|---|---|---|---|---|
| Privilege escalation via capabilities | GTFOBins cap abuse cap_setuid+ep | getcap -r / 2>/dev/null; flag cap_setuid, cap_sys_admin, cap_net_raw on unexpected binaries | FS-016: getcap -r / checked; escalation/noteworthy caps differentiated | Done |
| PATH hijacking | Writable dir earlier in root's PATH; cron/systemd executes relative command | Check $PATH and cron env for writable dirs; check systemd units for relative ExecStart | IDENT-026: PATH scanned for world-writable entries; systemd relative ExecStart flagged | Done |
| Sudo token reuse / abuse | Timestamp caching; sudo -l exposes shell-escape binaries | Check timestamp_timeout in sudoers; enumerate ALL sudo rules for GTFOBins patterns beyond the 8 hardcoded | IDENT-027: timestamp_timeout checked for long/disabled. IDENT-028: comprehensive GTFOBins list (45+ commands) | Done |
| Weak service credentials | Default creds in MySQL, Redis, MongoDB, Postgres configs | Check for empty/default passwords in /etc/mysql/, ~/.pgpass, Redis requirepass, MongoDB auth | IDENT-025: MySQL plaintext config, Redis requirepass, Postgres trust auth & .pgpass perms | Done |
| Exposed secrets in env / files | API keys, tokens, passwords in /etc, home dirs, shell history, /proc/\*/environ | Scan for high-entropy strings and common secret patterns in readable config files and shell history | FS-018: regex-based secret scanning in shell histories, .env, .netrc | Done |
| Cron / timer injection | Writable cron script or systemd timer unit; attacker modifies to execute payload | Check permissions of cron scripts, not just their contents; check all timer ExecStart paths are root-owned | FS-007: checks cron permissions (owner + world-writable) on all cron dir files | Done |
| SSH agent / key forwarding abuse | Check AllowAgentForwarding and AllowTcpForwarding in sshd_config | IDENT-023: agent forwarding; IDENT-024: TCP forwarding | Done |
| Writable /etc/passwd or /etc/shadow | Attacker adds UID-0 entry or replaces password hash directly | Check permissions on all auth files: /etc/passwd, /etc/group, /etc/shadow, /etc/gshadow | FS-006: covers all 5 critical auth files (shadow, gshadow, sudoers, passwd, group) | Done |
| Kernel exploit prerequisites | Dirty COW, overlayfs, user namespace exploits require specific conditions | Check kernel version against known-vulnerable ranges; check kernel.unprivileged_userns_clone | KERN-029: uname -r matched against 15+ known-vuln ranges (Dirty Pipe, Dirty COW, nf_tables, etc.) | Done |
| NFS / SMB share misconfiguration | no_root_squash on NFS export lets remote root write as root locally | Parse /etc/exports for no_root_squash, world-accessible shares, insecure options | FS-019: no_root_squash, insecure, world-accessible detected. FS-020: Samba security mode + guest access | Done |
| Writable /etc/ld.so.preload | Attacker injects shared library loaded into every process | Check existence and permissions of /etc/ld.so.preload and /etc/ld.so.conf.d/ | FS-017: ld.so.preload/ld.so.conf/ld.so.conf.d/ checked for ownership, writability, and existence | Done |
